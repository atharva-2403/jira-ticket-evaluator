{
  "pr_url": "https://github.com/org/repo/pull/303",
  "title": "refactor: introduce repository pattern",
  "description": "Moved SQL logic to repository.py and injected into routes.",
  "files_changed": [
    "src/database/repository.py",
    "src/api/routes.py",
    "tests/test_repository.py"
  ],
  "commit_messages": [
    "refactor: create UserRepository",
    "feat: inject repo into routes",
    "test: repo unit tests"
  ],
  "diff": "diff --git a/src/api/routes.py b/src/api/routes.py\n-    db.execute('SELECT * FROM users')\n+    return user_repo.get_all()\ndiff --git a/src/database/repository.py b/src/database/repository.py\n+class UserRepository:\n+    def get_all(self):\n+        return self.db.execute('SELECT * FROM users')"
}
